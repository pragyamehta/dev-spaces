# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

bikesharingweb:
  ingress:
    annotations:
      kubernetes.io/ingress.class: addon-http-application-routing
    hosts:
      - dev.bikesharingweb.1d77cd06b2c648259964.westus2.aksapp.io  # Assumes deployment to the 'dev' space

gateway:
  ingress:
    annotations:
      kubernetes.io/ingress.class: addon-http-application-routing
    hosts:
      - dev.gateway.1d77cd06b2c648259964.westus2.aksapp.io  # Assumes deployment to the 'dev' space