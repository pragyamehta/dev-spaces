# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

bikesharingweb:
  ingress:
    annotations:
      kubernetes.io/ingress.class: addon-http-application-routing
    hosts:
      - dev.bikesharingweb.6c1afd732423493cb507.eastus2.aksapp.io  # Assumes deployment to the 'dev' space

gateway:
  ingress:
    annotations:
      kubernetes.io/ingress.class: addon-http-application-routing
    hosts:
      - dev.gateway.6c1afd732423493cb507.eastus2.aksapp.io  # Assumes deployment to the 'dev' space